apiVersion: v1
kind: Service
metadata:
  name: postgres-db-headless
  namespace: parcelhub
spec:
  clusterIP: None
  selector:
    app: postgres-db
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432